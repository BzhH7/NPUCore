# OS Kernel Demo Applications

本目录包含用于演示操作系统内核功能的各种应用程序。

## 目录结构

```
apps/
├── Makefile          # 主构建文件
├── README.md         # 本文件
├── kilo/             # 文本编辑器
├── tetris/           # 俄罗斯方块游戏
├── snake/            # 贪吃蛇游戏
├── 2048/             # 2048数字游戏
├── utils/            # 系统工具集
├── bench/            # 性能基准测试
└── demo/             # 演示启动器
```

## 快速开始

### 1. 编译所有应用

```bash
cd apps
make
```

### 2. 安装到文件系统

```bash
# RISC-V 64位架构
make install ARCH=riscv64

# 或 LoongArch 64位架构
make install ARCH=la64
```

### 3. 运行内核

编译并运行内核后，在shell中执行:

```bash
# 启动演示菜单
/demo

# 或直接运行单个程序
/snake
/tetris
/2048
/kilo myfile.txt
/bench
```

## 应用列表

### 🎮 游戏

| 程序 | 描述 | 演示功能 |
|------|------|----------|
| `tetris` | 俄罗斯方块 | termios, 非阻塞I/O, 定时器 |
| `snake` | 贪吃蛇 | 实时输入, ANSI渲染, 随机数 |
| `2048` | 数字合并游戏 | 键盘输入, 彩色输出 |

### 📝 应用程序

| 程序 | 描述 | 演示功能 |
|------|------|----------|
| `kilo` | 文本编辑器 | 文件I/O, 终端控制, 语法高亮 |

### 🔧 系统工具

| 程序 | 描述 | 演示功能 |
|------|------|----------|
| `cat` | 显示文件内容 | read/write 系统调用 |
| `echo` | 输出文本 | 命令行参数处理 |
| `wc` | 统计文件 | 文件读取, 字符处理 |
| `tree` | 目录树 | getdents64, stat, 递归 |
| `cal` | 日历 | gettimeofday, 时间计算 |
| `hexdump` | 十六进制查看 | 文件读取, 格式化输出 |
| `uptime` | 系统运行时间 | 时间系统调用 |

### 📊 基准测试

| 程序 | 描述 | 测试项目 |
|------|------|----------|
| `bench` | 性能测试套件 | syscall, fork, mmap, 文件I/O, pipe, yield |

### 🚀 演示启动器

| 程序 | 描述 |
|------|------|
| `demo` | 交互式菜单，一键启动所有演示程序 |

## 内核功能覆盖

这些应用程序演示了以下内核功能:

- **进程管理**: fork, exec, wait, exit, getpid
- **内存管理**: mmap, munmap, brk
- **文件系统**: open, close, read, write, lseek, stat, getdents64
- **终端I/O**: termios (tcgetattr/tcsetattr), ioctl
- **时间**: gettimeofday, nanosleep/usleep
- **信号**: 基本信号处理
- **进程间通信**: pipe

## 依赖

编译需要交叉编译工具链:

- **RISC-V**: `riscv64-linux-gnu-gcc`, `riscv64-linux-gnu-g++`
- **LoongArch**: `loongarch64-linux-gnu-gcc`, `loongarch64-linux-gnu-g++`

所有程序使用静态链接 (`-static`)，无需动态库支持。
